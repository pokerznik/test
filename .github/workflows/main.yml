# This is a basic workflow to help you get started with Actions

name: Deploy via ftp
on: push
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: "167.86.118.244"
        user: "egardening_dev"
        password: "ppt66Dfd5VCSgmhd?"
        localDir: "publish"
        remoteDir: "var/www/projects/eGardening_Development/travellist/public"
